extends layout
//- We have also added client-side validations into our HTML form. 
//- This will prevent people from even submitting a form that has invalid data
//- Added * to give a visual way to denote a required field!
block content
    
  .row.justify-content-center
    .col-sm-6
      legend New Pet
      form(action="#" id="new-pet", method="post")
        .form-group
            label Name
            span.required.text-danger *
            input.form-control(name="name" required)
        .form-group
            label Birthday
            span.required.text-danger *
            input.form-control(name="birthday" required)
        .form-group
            label Species
            span.required.text-danger *
            input.form-control(name="species" required)
        .form-group
            label Avatar
            span.required.text-danger *
            input.form-control(name="avatar" type="file" required)
        .form-group
            label Favorite Food
            span.required.text-danger *
            input.form-control(name="favoriteFood" required) 
        .form-group
            label Description
            span.required.text-danger *
            textarea.form-control(name="description" required minlength="140")
        .form-group
            label Price
            span.required.text-danger *
            textarea.form-control(name="price" required type="number")  
        button(type="submit").btn.btn-primary Save